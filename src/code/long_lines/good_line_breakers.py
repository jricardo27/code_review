# Parenthesis. Case 1.
if (something == True and
        something_else == False):


# Parenthesis. Case 2.
string = (
    'This string is too long to fit.'
)

string = (
    'This string is too long to fit.'
    ' This one is even longer.'
)


# Parenthesis. Case 3.
assert val > 4, (
    "val is too small")

assert val > 4, (
    "val is too small"
)
